# Version of this plugin. Used for the artifact name and embedded in ver.json.
pluginVersion=0.1.0

# Version of the TAK Server plugin SDK (takserver-plugins) that this plugin
# is compiled against. Must match the SDK published by the TAK Server build
# (e.g. `./gradlew :takserver-plugins:publishToMavenLocal` from the TAK Server
# source tree) or staged into your local Maven repository.
takserverPluginsVersion=5.4-RELEASE

# Library versions
jacksonVersion=2.17.2
slf4jVersion=2.0.13
junitVersion=5.10.2

# Build settings
org.gradle.jvmargs=-Xmx1024m

package tak.server.plugins;

/** Compile-only stub of {@code tak.server.plugins.MessageSenderBase}. */
public abstract class MessageSenderBase extends PluginBase implements MessageSender {
    protected MessageSenderBase() throws ReservedConfigurationException {
        super();
    }
}
